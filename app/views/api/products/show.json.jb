#image = Image.find_by(product_id: @product.id)
render partial: "product.json.jb", locals: {product: @product, image: @image}

  # {
  #   id: @product.id,
  #   name: @product.name,
  #   price: @product.price,
  #   image_path: @product.image_path, 
  #   description: @product.description,
  #   formatted_data:{
  #     is_discounted?: @product.is_discounted?,
  #     tax: @product.tax,
  #     total: @product.total
  #   }
  # }
